```tsx
export default function AddItem(props: AddItemProps) {
  const [open, setOpen] = useState(false);
  const [item, setItem] = useState<Item>({
    product: '',
    amount: '',
    price?: 0,
  });
```
위의 `pirce?` 와 같이 뒤에 ? 붙이면 optional이 됨.

- todolist2 는 기존과 다르게 lucide-react가 아니라 mui 내에서도 아이콘이 존재하기 떄문에 추가로 의존성을 추가하겠습니다.

npm install @mui/icons-material@5.16.14

```tsx
// 삭제
  const handleDeleteTodo = (id: number) => {
    setTodos(todos.filter(todo => todo.id !== id));
  }
```
filter 에서 위 내용은 todo.id가 같지 않으면 덮어쓴다는 말 (DELETE와 동일한 기능의 역할)

carfront 프로젝트명으로 react/typescript로 만들고
axios 설치 / @tanstack/react-query@4 설치

```tsx
import { Container, AppBar, Toolbar, Typography, CssBaseline } from "@mui/material"

function App() {
  
  return (
    <Container maxWidth='xl'>
    <CssBaseline />
      <AppBar position="static">
        <Toolbar>
          <Typography variant="h6">
            Car Shop
          </Typography>
        </Toolbar>
      </AppBar>
    </Container>
  )
}

export default App
```
- 저희가 다양한 코드에서 확인했듯이 MUI Container 컴포넌트 내에 AppBar를 하위 컴포너느로 위치시켰습니다(props drilling 개념 때문에 매우 중요합니다). position prop을 이용하여 AppBar의 위치를 static으로 잡았스빈다.
  - static : 스크롤 할 때 앱 바가 상단에 고정되지 않고 사라지게 됩니다.
  - fixed : 스크롤 해서 넘어가더라도 앱 바가 상단에 남아있습니다.

  - maxWidth : 영어 읽으면 답 나오고, 이미 사용해봄
  (camelCase인 것만 신경쓰겠습니다)

  - CssBaseline 컴포넌트를 이용하여 여러 브라우저에서 리액트 앱의 형태가 균일하게 유지되도록 설정했습니다. 일반적으로 최상단에 위치시켜 하위 컴포넌트들이 동일한 영향을 받도록 사용합니다.

  - 여기서 AppBar의 position을 속성이라고 하지 않고 프롭이라고 이야기한 부분에 주목해야 함. html에서의 속성은 attribute였습니다. 하지만 props drilling에서의 props의 full word는 properties입니다.

# CRUD 기능 추가하기

## GET 요청하기
- QueryClientProvider 사용하겠습니다.

- 이상의 컴포넌트를 import하겠다고 했다면 다음 작업이 무엇과 관련되었는지 이해할 수 있으면 좋겠습니다.

```tsx
import { useQuery } from "@tanstack/react-query";
import { CarResponse } from "../types";
import { getCars } from "../api/carapi";


export default function Carlist () {
  

  const {data, error, isSuccess} = useQuery({
    queryKey: ['cars'], 
    queryFn: getCars
  });

  if(!isSuccess) {
    return <span>Loading ...</span>
  }
  else if (error) {
    return <span>자동차 데이터를 가져오던 중 오류가 발생했습니다...</span>
  }
  else {
    return (
      <table>
        <tbody>
          {
            data.map((car: CarResponse) => 
              <tr key={car._links.self.href}>
                <td>{car.brand}</td>
                <td>{car.model}</td>
                <td>{car.color}</td>
                <td>{car.registrationNumbere}</td>
                <td>{car.modelYear}</td>
                <td>{car.price}</td>
              </tr>
            )
          }
        </tbody>
      </table>
    )
  }
}
```